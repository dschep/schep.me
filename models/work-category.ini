[model]
name = Work Category
label = {{ this.name }}
hidden = yes
protected = yes

[children]
model = work-entry
order_by = order

[fields.name]
label = Name
type = string

[fields.order]
label = Order
type = sort_key
